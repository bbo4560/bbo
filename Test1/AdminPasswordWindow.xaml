<Window x:Class="Test1.AdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="刪除驗證"
        Height="180"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Microsoft JhengHei"
        Background="{StaticResource MaterialDesignPaper}"   
    >
    <Grid Margin="20,10,20,0">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#f4f7fb" Offset="0" />
                <GradientStop Color="#dde6f7" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="請輸入密碼"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"
                   Foreground="#333" Cursor=""/>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="1"
                     Height="32"
                     FontSize="16"
                     Padding="8,4"
                     KeyDown="PasswordBox_KeyDown"/>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,18,0,0">
            <Button Content="確認"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="Confirm_Click"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Style="{StaticResource MaterialDesignRaisedButton}" Cursor="Hand"/>
            
        </StackPanel>
    </Grid>
</Window>

